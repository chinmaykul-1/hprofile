name: Test workdflow
on: workflow_dispatch
jobs:
  Checkoutandtest:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: mvn test
        run: mvn test

      - name: mvn checkstyle
        run: mvn checkstyle:checkstyle
